package transport;
public class Bus extends Transport{

    public Bus(String brand, String model, String country, String color, int year, int maxSpeed) {
        super(brand, model, country, color, year, maxSpeed);
    }
}
